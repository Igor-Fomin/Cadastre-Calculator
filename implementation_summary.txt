Refactoring Summary: Smart Sync Implementation

1.  **UI Update:**
    *   Replaced `btnRedraft` and `btnHeal` with `btnSync` (Icon: 🔄).
    *   Updated Grid layout in `InitializeCustomUI`.

2.  **Logic Consolidation:**
    *   Implemented `SmartReconcile_Click` method in `Class1.cs`.
    *   This method performs a 4-step reconciliation:
        1.  **Deep Cleanup:** Updates plugin database from drawing geometry (Source of Truth).
        2.  **Ghost Erasure:** Removes old text entities to prevent duplicates.
        3.  **Restore Missing:** Re-creates missing line entities from database.
        4.  **Fresh Annotation:** Regenerates all text labels (Bearing, Distance, Point #, Comments).

3.  **Cleanup:**
    *   Removed obsolete methods: `RedraftTraverses_Click`, `HealAndSync_Click`, and `RedraftVisibleTraverses`.

Status: Complete.
